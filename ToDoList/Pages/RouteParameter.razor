@page "/route-parameter/{text?}"

<h1>Parámetro de Ruta: @Text</h1>


@code {
    [Parameter]
    public string? Text { get; set; }
    protected override void OnInitialized()
    {        
        Text = Text ?? "default";
    }
}
